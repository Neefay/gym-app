
const GENERIC = require("./generic").STRING_LIST

const USERS = require("../Modules/User/strings").STRING_LIST

const STRING_LIST = {
    RESPONSES: {
        GENERIC,

        USERS
    }
}

module.exports = { STRING_LIST }